<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ZakatGoldApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/goldPrimary</item>
        <item name="colorPrimaryDark">@color/gold_background</item>
        <item name="colorAccent">@color/goldPrimary</item>
        <item name="android:statusBarColor">@color/gold_background</item>
        <item name="android:navigationBarColor">@color/goldPrimary</item>
        <item name="android:textColor">@color/textDark</item>
    </style>

    <style name="ToolbarTheme" parent="Widget.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/black</item>
        <item name="actionMenuTextColor">@color/black</item>
    </style>

    <style name="Theme.ZakatGoldApplication" parent="Base.Theme.ZakatGoldApplication" />
</resources>